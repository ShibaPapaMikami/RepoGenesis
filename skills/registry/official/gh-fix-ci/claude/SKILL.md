# GH Fix CI

Use this skill when GitHub Actions is failing and the project needs a focused recovery plan.

Check:
- which job failed
- whether the error is deterministic
- which local command matches the workflow step
- whether the fix should be code, test, or config

Return:
1. root cause
2. smallest safe fix
3. follow-up items
